# --- Day 1: web, db, auth ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy==2.0.36
# PyJWT, not python-jose: jose has been unmaintained since 2021 and carries
# CVE-2024-33663 (algorithm confusion) and CVE-2024-33664 (JWE decompression
# bomb). Dropping it also removes cryptography, ecdsa, rsa and pyasn1, none of
# which anything else imports.
PyJWT==2.10.1
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
email-validator==2.2.0

# --- Day 2: knowledge files ---
pyyaml==6.0.2

# --- Day 5: RAG + embeddings (CPU) ---
sentence-transformers==3.3.1
numpy==2.2.1

# --- Day 6: documents + reports ---
pdfplumber==0.11.5
# weasyprint replaced by fpdf2: WeasyPrint needs GTK system libraries
# (libgobject) that are not available on the target Windows machine.
fpdf2==2.8.2

# --- Day 7: tests ---
pytest==8.3.4
httpx==0.28.1
