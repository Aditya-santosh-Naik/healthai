# Python
__pycache__/
*.py[cod]
.venv/
backend/healthai.db
backend/uploads/
backend/reports_out/

# Built artefacts
data/index.npz
backend/.secret_key

# Secrets. config.py reads backend/.env via SettingsConfigDict(env_file=".env"),
# so anyone moving a key into an env var lands it here. Ignored BEFORE anyone
# creates one -- a secret committed once stays in history whatever happens
# next.
.env
*.env
!.env.example

# Node
node_modules/
frontend/dist/
*.tsbuildinfo

# Editors / OS
.DS_Store
.vscode/
.idea/
test-files/
